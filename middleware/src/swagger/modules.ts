import { DocumentModule } from '../inventory/inventory.module';

const modules = [
  {
    module: DocumentModule,
    options: {
      resourcePath: 'DocumentClosure',
      title: 'Document Closure',
      description: 'Api Document Closure',
      version: '1.0',
      tag: 'DocumentClosure',
    },
  },
];

export default modules;
